// ==========================================
// CONCAT
// ==========================================

// Concat is used to join multiple arrays. It’s not destructive, so it returns a copy of the arrays that it joins.

var array1 = ["Hello"];
var array2 = [" World!"];
var output = array1.concat(array2); 
//output: ["Hello", " World!"]