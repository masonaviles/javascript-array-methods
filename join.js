// ==========================================
// JOIN
// ==========================================

// Join joins all elements of an array into a string. It also allows a separator to be specified.

var array = ["Look", "at", "all", "the", "smiles!"];
var joined = array.join(" :) ");
//joined: "Look :) at :) all :) the :) smiles!""